{
  "name": "stomatiq",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "dev:site": "npm run dev --workspace=@stomatiq/site",
    "dev:board": "npm run dev --workspace=@stomatiq/board",
    "build:site": "npm run build --workspace=@stomatiq/site",
    "build:board": "npm run build --workspace=@stomatiq/board"
  },
  "type": "module",
  "engines": {
    "node": ">=18"
  }
}
